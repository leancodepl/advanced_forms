/*
 * AI-Provenance:
 *   model: Claude Fable 5.1
 *   harness: Claude Code
 */
import { themes } from "@/lib/palette.generated"

export { swatches, themes, withAlpha } from "@/lib/palette.generated"

/** The dark theme: what the social cards are drawn in, whatever the reader's theme. */
export const dark = themes.dark
